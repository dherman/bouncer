## Formatting

Before pushing to a PR branch, always run `pnpm run format:check` and fix any issues with `pnpm run format` (which runs Prettier). This applies to all file types Prettier covers, includ...{truncated}